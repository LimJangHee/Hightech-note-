* 동명의 파일이 존재할 수 있기 때문에 구분자로서 패키지를 사용하는 것 -> 3level로 사용하는 것이 일반적(~.~.~)
-> kopo.ac.kr이라는 (회사)도메인을 예로 들면 ex)역으로
-> kr.ac.kopo라는 패키지를 사용 : 이 경우 쩜 단위로 폴더를 생성함
ex) 서울대랑 같으했으면 kopo 부분에 suv폴더, 정부랑 같이 했으면 ac부분에 gov폴더, 외국이랑 했으면 kr부분에 us 폴더 등

* Java는 대,소문자 구분 있음 -> class이름 맨 처음에 대문자 쓰는거 제외하고는 다 소문자 사용
ex) public class Hello

* 다른 사람의 코드를 가져올때는 패키지 디렉토리 확인할 것
->오류나는 메인 이유 중 하나

* main() -> main 메소드 : 괄호가 있을시 이를 메소드라고 하며 그 앞이 main이라서 main메소드 

* 문장 구분자는 ;

* 들여씌기가 안되어 있는경우 ctrl+shift+f를 하면 자동 포멧팅 해줌

* 주석문 : 코드 설명용 
- /* ~ */
- //
- /** ~ */ : 문서화주석 -> 설명서같은 주석

* main 메소드를 가지고 있는 클래스에는 뒤에 Main을 붙여줌

* 문자 : '~', '\n' : 개행,  '\t' : tap, '\\' : 역슬러쉬

* 문자열 : "~"

* 아스키코드는 영어만 포함됨 : 한글, 중국어, 일본어 등 없음
-> 그래서 생긴게 유니코드(java는 유니코드)

* 문자열끼리 더할때 + 사용 단! 문자 + 문자를 할 시에는 아스키(유니)코드로 인식해서 숫자로 나옴
-> 즉 그래서 print메소드류 출력을 할때는 문자열을 사용

* 싹 다 대문자로 구성되어 있으면 상수변수
멤버변수의 접근제한자는 private으로
메소드의 접근제한자는 public으로

public : 패키지에 상관없이 누구나 접근 가능
protected : 같은 패키지에서 접근 가능, 단 다른패키지에서 상속관계가 있는 경우 접근 가능
default : 같은 패키지내에서만 접근 가능
private : 클래스내에서만 접근 가능

접근제한자를 붙이지 않으면 기본값은 default
